SUDO_PASSWORD=
# Si Ubuntu reporta error de certificado TLS con el portal MSP,
# habilita temporalmente esta bandera para pruebas:
COBERTURA_TLS_INSECURE=true
# Alternativa segura: ruta a un bundle PEM confiable.
# COBERTURA_CA_FILE=/ruta/al/ca_bundle.pem
